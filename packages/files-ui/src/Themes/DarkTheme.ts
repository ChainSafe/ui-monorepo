import { createTheme } from "@imploy/common-themes"

export const darkTheme = createTheme({
  themeConfig: {
    palette: {
      primary: {
        main: "#FFF",
      },
      secondary: {
        main: "#000",
      },
    },
  },
})

import { createTheme } from "@chainsafe/common-theme"

export const darkTheme = createTheme({
  themeConfig: {
    palette: {
      primary: {
        main: "#FFF"
      },
      secondary: {
        main: "#000"
      }
    }
  }
})
